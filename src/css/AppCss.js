import styled from '@emotion/styled'
import { size } from 'polished'

export const AppCss = styled.div`
  ${size('inherit')};
`

export const AppHeaderCss = styled.header`
  display: flex;
  background-color: #282c34;
  min-height: 100vh;
  width: inherit;
  color: white;
`